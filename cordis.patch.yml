# Plugin patch layer: insert the petween-physics companion entry into the
# profile's cordis tree. `dsh plugin add` picks this file up via the
# package.json `dsh.bundle.patch` field.
- insert:
    - id: petween-physics
      name: 'petween-physics'
